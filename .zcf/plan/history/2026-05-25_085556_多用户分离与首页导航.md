# 任务：多用户数据分离 + 首页导航

## 状态：已完成 ✅
## 完成时间：2026-05-25

## 任务描述
将两个不同用户的数据按用户分开显示，建立首页导航页。

## 执行步骤

| 步骤 | 文件 | 说明 |
|------|------|------|
| 1 | `src/rhythmind/api/routers/dashboard.py` | 新增 `GET /qm/api/users/summary` API |
| 2 | PostgreSQL | 插入 athlete_zhang 完整数据（16 facts + 3 诊断 + 6 事件 + 3 用药 + 13 化验） |
| 3 | `frontend/src/lib/api.ts` | 统一导出 getAuthToken/setAuthToken/API_BASE/V1_BASE |
| 4 | `frontend/src/app/page.tsx` | 首页改为用户选择导航页 |
| 5 | `frontend/src/components/layout/header.tsx` | Header 增加用户标识 + /medical 导航 |
| 6 | `frontend/src/app/medical/page.tsx` | 复用 api.ts，labs 改用全量接口 |
| 额外 | `src/rhythmind/api/routers/medical.py` | 新增 `GET /medical/labs`（纯数据，无 LLM） |

## 优化记录

| # | 优化项 | 收益 |
|---|--------|------|
| 1 | Header 用户显示改为 localStorage 缓存驱动 | 新增用户无需改前端 |
| 2 | 删除 medical 未使用的 Diagnosis 接口和 state | 清除死代码 |
| 3 | /users/summary N+1 查询合并为 6 条批量 SQL | O(N)→O(1) 数据库往返 |
| 4 | 移除循环内冗余 `import json as _json` | 使用顶部已有 import |
| 5 | /users/summary 保持无鉴权 | 首页未登录时可用 |

## 验证结果
- 构建通过，0 TypeScript 错误，0 console errors
- 首页双用户卡片正确渲染
- 用户切换后 dashboard/medical 数据正确隔离
- Header 用户标识正确显示（从缓存读取）
