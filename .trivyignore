# ─────────────────────────────────────────────────────────────────────────────
# .trivyignore — Trivy 镜像扫描豁免列表（CVE-xxxx 等）
# ─────────────────────────────────────────────────────────────────────────────
# 格式：一行一个 CVE ID。注释以 # 起。
# 维护规则同 .pip-audit-allowlist：每条都要附 expiry + 工单链接（注释里写）。
#
# 仅用于"已知 Trivy 误报"或"上游补丁未发布、风险已评估为低"的场景。
# 业务自身的依赖漏洞应通过升级 poetry.lock 修复，不要丢进这里。
#
# 示例:
#   CVE-2024-XXXXX   # debian libxml2; expiry 2026-08-01, issue #51
# ─────────────────────────────────────────────────────────────────────────────

# 当前无豁免。
