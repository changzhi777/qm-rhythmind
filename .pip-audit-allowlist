# ─────────────────────────────────────────────────────────────────────────────
# .pip-audit-allowlist — 暂时豁免的 pip-audit vulnerability ID
# ─────────────────────────────────────────────────────────────────────────────
# 格式: 一行一个 ID，井号注释。每条豁免必须附 expiry + issue 链接。
# CI 会把每行翻译成 `--ignore-vuln <ID>`。
#
# 流程:
#   1. CI 失败时拿到具体 GHSA-xxx / PYSEC-xxx ID
#   2. 评估：是否真受影响？是否有补丁？
#   3. 若需短期豁免：在此追加并附 expiry/issue
#   4. 到期前必须 review；不再有效则升级依赖或换替代品
#
# 示例:
# GHSA-xxxx-xxxx-xxxx  # expiry 2026-09-01, issue #42 — upgrade blocked by autogen 0.7
# ─────────────────────────────────────────────────────────────────────────────

# 当前无豁免。
